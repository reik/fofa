# WCAG 2.1 AA Audit Report — 2026-05-15

## 1. Images missing alt / non-descriptive alt

| File | Violation | Fix |
|---|---|---|
| `src/components/family/FamilyMemberCard.tsx` | `alt="thumbnail"` — generic | Changed to `alt={`${existing?.name ?? "Family member"} photo`}` |

## 2. Inputs without labels

| File | Violation | Fix |
|---|---|---|
| `src/pages/RegisterPage.tsx` | State `<select>` unlinked from label | Added `htmlFor="register-state"` / `id="register-state"` |
| `src/pages/ProfilePage/ProfileForm.tsx` | State `<select>` unlinked from label | Added `htmlFor="profile-state"` / `id="profile-state"` |
| `src/pages/MemberProfilePage.tsx` | Message `<textarea>` had no label | Added `aria-label="Write a message to {firstName}"` |
| `src/pages/MemberProfilePage.tsx` | Playdate request `<textarea>` unlinked from label | Added `htmlFor="playdate-request-message"` / `id="playdate-request-message"` |
| `src/pages/PlaydatesPage.tsx` | Date input unlinked from label | Added `htmlFor="slot-date"` / `id="slot-date"` |
| `src/pages/PlaydatesPage.tsx` | Note input unlinked from label | Added `htmlFor="slot-note"` / `id="slot-note"` |

## 3. `outline-none` without focus-visible replacement

All instances had `focus-visible:ring-2 focus-visible:ring-brand` added alongside `outline-none`.

| File |
|---|
| `src/components/ui/Input.tsx` |
| `src/components/announcements/CreateAnnouncementForm.tsx` |
| `src/components/messages/MessagesPanel.tsx` |
| `src/pages/CommunityPage.tsx` |
| `src/pages/MemberProfilePage.tsx` |
| `src/pages/PlaydatesPage.tsx` |
| `src/pages/RegisterPage.tsx` |
| `src/pages/ProfilePage/ProfileForm.tsx` |
| `src/components/playdates/TimePicker.tsx` |

## 4. Modal ARIA on wrong element

| File | Violation | Fix |
|---|---|---|
| `src/components/ui/Modal.tsx` | `role="dialog"`, `aria-modal`, `aria-labelledby` on backdrop `div` | Moved to inner content container; backdrop now uses `role="presentation"` |

## 5. `onClick` on non-interactive elements

| File | Violation | Fix |
|---|---|---|
| `src/components/playdates/WeekCalendar.tsx` | Clickable day-background `<div onClick>` | Replaced with `<button type="button" aria-label="Add availability slot on {iso}">` |
| `src/components/playdates/WeekCalendar.tsx` | Slot block `<div onClick>` | Replaced with `<button type="button" aria-label="...">` |
| `src/components/playdates/MonthCalendar.tsx` | Day cell `<div onClick>` | Added `role="button"`, `tabIndex={0}`, `aria-label`, `onKeyDown` |

## Not changed / needs manual review

- `src/components/Navabar.tsx` — legacy unused stub; no violations
- `src/components/test/*` — excluded (scratch files, not production UI)
- Badge `<span>` elements in `Navbar.tsx` — inside `<Link>` with `aria-label` on parent; no action needed
